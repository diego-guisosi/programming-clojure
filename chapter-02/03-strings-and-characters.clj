;Multi-line strings
"This is a \nmulti-line string"
"This is also a
multi-line string"

;str function - call toString of all parameters and concatenates them. nil is ignored
(str 1 2 nil 3)

;characters literals are preceded by \
(str \h \e \y \space \y \o \u)
